# Secret IDs released by the Vault DON into the enclave.
# Values come from environment variables (cre/.env locally, never committed).
secretsNames:
  evidence_api_key:
    - STICKERBOMB_EVIDENCE_API_KEY
  geofence_salt:
    - STICKERBOMB_GEOFENCE_SALT
